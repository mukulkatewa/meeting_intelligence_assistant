Sample meeting files are generated locally (not committed by default).

Run:

```bash
npm run generate-sample
```

Generated files:

- `generated/deck.pdf`
- `generated/meeting_audio.wav`
- `generated/screen_recording.mp4`
- `generated/transcript.json` (reference transcript used in demo script)

These files are aligned so cross-modal questions in the README produce grounded answers.
